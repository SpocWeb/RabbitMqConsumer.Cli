{ // Picked up automatically; make sure to set "Copy Always" if you have appSettings.json in multiple Projects to avoid the 'newest' to 'win' in the 'bin' Folder!
	"RabbitMqConfig": {
		"Host": "localhost",
		"Path": "",
		"UserName": "guest",
		"PassWord": "guest",
		"ConcurrencyLimit": 1
	}
}